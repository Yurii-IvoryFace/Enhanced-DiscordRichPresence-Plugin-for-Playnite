<UserControl x:Class="DiscordRichPresencePlugin.Views.TemplateManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
	<Grid Margin="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Шаблони Rich Presence" FontWeight="Bold" FontSize="14"/>

		<DataGrid Grid.Row="1" Margin="0,8,0,8"
                  ItemsSource="{Binding Templates}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="2*" IsReadOnly="True"/>
				<DataGridTextColumn Header="Назва" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
				<DataGridTextColumn Header="Опис" Binding="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="4*"/>
				<DataGridCheckBoxColumn Header="Увімкн."
                                        Binding="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Width="*"/>
				<DataGridTextColumn Header="Пріор."
                                    Binding="{Binding Priority, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*"/>
			</DataGrid.Columns>
		</DataGrid>

		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button x:Name="BtnRefresh" Content="Оновити" Margin="0,0,8,0" Click="BtnRefresh_Click"/>
			<Button x:Name="BtnExport" Content="Експорт…" Margin="0,0,8,0" Click="BtnExport_Click"/>
			<Button x:Name="BtnImport" Content="Імпорт…" Margin="0,0,8,0" Click="BtnImport_Click"/>
			<Button x:Name="BtnSave" Content="Зберегти" IsDefault="True" Click="BtnSave_Click"/>
		</StackPanel>
	</Grid>
</UserControl>
