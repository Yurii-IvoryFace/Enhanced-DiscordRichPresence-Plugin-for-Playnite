<UserControl x:Class="DiscordRichPresencePlugin.Views.TemplateManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
	<Grid Margin="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Шаблони Rich Presence" FontWeight="Bold" FontSize="14"/>

		<DataGrid Grid.Row="1" Margin="0,8,0,0" AutoGenerateColumns="False"
                  ItemsSource="{Binding Templates}" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="2*"/>
				<DataGridTextColumn Header="Назва" Binding="{Binding Name}" Width="2*"/>
				<DataGridTextColumn Header="Опис" Binding="{Binding Description}" Width="4*"/>
				<DataGridCheckBoxColumn Header="Увімкн." Binding="{Binding IsEnabled}" Width="*"/>
				<DataGridTextColumn Header="Пріор." Binding="{Binding Priority}" Width="*"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
